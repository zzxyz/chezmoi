{{- /* Ignore .zsharch on non-Arch systems */ -}}
{{- if and (hasKey .chezmoi "osRelease") (hasKey .chezmoi.osRelease "id") }}
{{-   if ne .chezmoi.osRelease.id "arch" }}
.zsharch
{{-   end }}
{{- else }}
{{- /* On Windows or systems without osRelease, always ignore .zsharch */ -}}
.zsharch
{{- end }}
